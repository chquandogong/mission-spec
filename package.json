{
  "name": "mission-spec",
  "version": "1.2.0",
  "description": "Mission Spec — AI 에이전트 워크플로를 위한 task contract layer",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp src/schema/mission.schema.json dist/schema/",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["mission-spec", "ai-agent", "task-contract", "claude-code-plugin"],
  "author": "Dr. QUAN",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "ajv": "^8.13.0",
    "yaml": "^2.4.0"
  }
}
